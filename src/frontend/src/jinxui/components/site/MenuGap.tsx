import React from "react"
import Box from "@material-ui/core/Box"
import Divider from "@material-ui/core/Divider"

const MenuGap = () => {
  return (
    <Box
      height="15px"
      display="flex"
      flexDirection="column"
      justifyContent="center"
    >
      <Divider />
    </Box>
  );
};

export default MenuGap
